#include <iostream>
using namespace std;
int main(){
    // Numbers are not allowed in the beginning. Can be in the mid or end!
    // Only _ and $ are allowed from special characters
    // Can start from alphabet 
    int x1 = 3;
    // int 1x = 4; --> will give an error 
    int y_  = 3;
    float $y = 2;
    float area = 2; // --> can be descriptive 
    int a = 3; // can be alphabets
    // Some particular keywords are not allowed
    // like int, book, long, auto, float, etc....
    // Commas, blanks are not allowed!
    return 0;
}